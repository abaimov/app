// const bcrypt = require("bcrypt");
// const { createPasswordHash } = require("../utils/passwordHash");

// exports.comparePasswordLogin = async (candidatePassword) => {
//   const passIsValid = bcrypt.compareSync(
//     candidatePassword,
//     await createPasswordHash(candidatePassword)
//   );
//   console.log("passIsValid", passIsValid);
//   return passIsValid;
// };
